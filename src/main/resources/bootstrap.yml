server:
  port: 8080

spring:
  cloud:
    config:
      uri: http://52.79.56.29:8888
      name: devStarMall-admin
      headers:
        application-name: devStarMall-admin
        application-password: "devStarMall"